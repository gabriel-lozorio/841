/**
 * @interface CountSequence
 * @description Represents a sequence of numbers, matching the backend type.
 * @property {number[]} numbers - Array of sequential numbers.
 */
export interface CountSequence {
  numbers: number[];
}
